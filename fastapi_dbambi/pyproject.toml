[project]
name = "fastapi-dbambi"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "tim-kornish-cloud", email = "kornish.timothy@gmail.com" }
]
requires-python = ">=3.13"
dependencies = [
    "fastapi[standard]>=0.141.1",
    "jinja2>=3.1.6",
    "pandas>=3.0.5",
]

[project.scripts]
fastapi-dbambi = "fastapi_dbambi:main"

[build-system]
requires = ["uv_build>=0.12.8,<0.13.0"]
build-backend = "uv_build"
